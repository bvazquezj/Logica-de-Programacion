#define PI 3.1416
float perimetrod(float d){
	float resultado;
	resultado = PI*d;
	return(resultado);
}

float perimetror(float r){
	float resultado;
	int s=2;
	resultado=s*PI*r;
	return(resultado);
	
}

